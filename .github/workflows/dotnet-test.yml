name: CI

on:
  pull_request:
    branches:
      - develop

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore nerv.log.sln

      - name: Build
        run: dotnet build nerv.log.sln --no-restore --configuration Release

      - name: Run tests
        run: dotnet test nerv.log.sln --no-build --configuration Release --verbosity normal
